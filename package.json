{
  "name": "test",
  "version": "1.0.0",
  "description": "A sample Node.js project",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "build": "NODE_ENV=production node dist/index.js serve build src",
    "test": "jest",
    "build:verbose": "npm run build --verbose",
    "dev": "nodemon --watch src --exec 'npm run start'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "init": "^0.1.2",
    "knex": "^2.4.2",
    "netlify-lambda": "^2.0.15",
    "pg": "^8.9.0",
    "serverless": "^3.27.0"
  },
  "engines": {
    "node": "16.x",
    "npm": "8.19.3"
  }
}
